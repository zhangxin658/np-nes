import numpy as np

# Number of paramters used in this evaluation module.
n_param = 10


def fitness_function(param):
    ''' Fitness evaluation by building an XOR neural network.

    Args:
        param: An np.ndarray that represents a list of parameters.
    Returns:
        A fitness value computed with error in XOR.
    '''

    return 0.0
